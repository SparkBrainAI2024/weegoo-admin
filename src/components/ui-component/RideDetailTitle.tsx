import { Typography } from '@mui/material';

const RideDetailTitle = ({ title }: { title: string }) => {
    return (
        <>
            <Typography variant="h5">{title}</Typography>
        </>
    );
};
export default RideDetailTitle;
